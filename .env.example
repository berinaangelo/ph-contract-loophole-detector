# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real credentials.

# Only needed for `cmd/server -llm-provider=claude`. Get a key from
# https://console.anthropic.com/settings/keys. Not needed for the default
# local (-llm-provider=ollama) setup.
ANTHROPIC_API_KEY=
